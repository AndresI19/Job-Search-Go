// Command jobsearch ingests job listings, verifies their legitimacy by
// cross-referencing ATS boards and Claude, and writes a scored CSV.
//
// This is a scaffold: the pipeline is wired up in later issues (see the
// "Let's Go!" project board).
package main

import "fmt"

func main() {
	fmt.Println("job-search-go: scaffold — pipeline not yet wired")
}
