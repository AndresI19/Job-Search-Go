{
  "name": "job-searcher-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Job Searcher web frontend — a Vite build that reuses @platform/ui (design tokens, base stylesheet, the sign-in gate and account FAB). The Go server serves the built assets under BASE_PATH.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fontsource/cormorant-garamond": "^5.3.0",
    "@platform/ui": "file:web/vendor/project-platform/portfolio-home/packages/platform-ui"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^5.4.11"
  }
}
